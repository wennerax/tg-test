{
  "name": "tg-moderation-bot",
  "version": "0.1.0",
  "description": "Telegram bot: users send JS questions; moderators answer; answers forwarded back.",
  "main": "index.js",
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=16"
  },
  "dependencies": {
    "telegraf": "^4.12.0",
    "dotenv": "^16.0.0"
  }
}
